<!DOCTYPE html>
<html>
  <head>
    <title>GameListApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="app_container">
      <% if flash[:notice] %>
        <p class="flash"><%= flash[:notice] %></p>
      <% end %>
      <% if @current_user %>
			  <p>ログイン中のユーザ：<%= @current_user.name %></p>
      <% end %>
      <h1>検索</h1>
      <%= form_tag("/search",:method => "get") do %>
        <input type="search" name="keyword" value=<%= @keyword %>>
        <button tyoe="submit">検索</button>
      <% end %>
      <% if @current_user %>
        <p><%= link_to("index画面へ","/") %></p>
        <p><%= link_to("登録情報の編集","/user/#{@current_user.id}/edit") %></p>
        <p><%= link_to("ログアウト","/logout") %></p>
      <% else %>
        <p><%= link_to("アカウント作成","/signup") %></p>
        <p><%= link_to("ログイン","/login_form") %></p>
      <% end %>
      <%
=begin%>
 <button class="js-modal-open">詳細検索</button> 
<%
=end%>
    </div>
    <div class="modal js-modal">
      <div class="modal__bg js-modal-close"></div>
      <div class="modal__content">
        <p>条件を選択してください</p>
        <p>ジャンル</p>
        <p>プラットフォーム</p>
        <p>発売年</p>
        <a class="js-modal-close" href="">閉じる</a>
      </div>
    </div>

    <%= yield %>
  </body>
</html>

<script type="text/javascript">
  $(function(){
    $(".js-modal-open").on("click",function(){
      $(".js-modal").fadeIn();
      return false;
    });
    $(".js-modal-close").on("click",function(){
      $(".js-modal").fadeOut();
      return false;
    })
  })
</script>
